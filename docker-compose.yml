services:
  frontend:
    build: .
    container_name: habitsdock-frontend
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://host.docker.internal:3000
    restart: unless-stopped